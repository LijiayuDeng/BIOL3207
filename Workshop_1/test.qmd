
```{r}
# 基本运算
x <- 1:10
mean(x)
sd(x)

# 创建数据框
df <- data.frame(
  name = c("Alice", "Bob", "Charlie"),
  score = c(85, 92, 78)
)
df

# 简单绘图
plot(x, x^2, type = "l", col = "steelblue",
     main = "y = x²", xlab = "x", ylab = "y")
```